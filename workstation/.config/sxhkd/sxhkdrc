KP_{Add,Subtract}
  pactl set-sink-volume @DEFAULT_SINK@ {+,-}2%

KP_Insert
  pactl set-sink-mute @DEFAULT_SINK@ toggle

super + {_,shift + }Return
  alacritty{_, --config-file .config/alacritty/alacritty-dark.yml}

super + d 
  rofi -show run

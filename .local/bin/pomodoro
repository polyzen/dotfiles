#!/usr/bin/env bash

timer() { termdown -T "$1" "$2" && (beep -f 5000 -l 50 -r 2; toastify send --id 45 "$3" "$4") }

pomodo=('Po' '  mo' '    do')
for o in "${pomodo[@]}"; do
  timer Work 25m Break "$o"
  timer "$o" 5m Work
done

timer Work 25m Long\ break '      ro'
timer ro 15m Pomodoro
